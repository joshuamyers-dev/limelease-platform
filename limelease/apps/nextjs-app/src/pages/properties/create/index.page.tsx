import CreatePropertyContainer from '@features/properties/createProperty/containers/CreatePropertyContainer';

const CreatePropertyPage = () => {
  return <CreatePropertyContainer />;
};

export default CreatePropertyPage;
