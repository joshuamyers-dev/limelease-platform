query myLease {
  myLease {
    ...LeaseBase
    property {
      files {
        ...FileBase
      }
      agents {
        id
        agent {
          id
          user {
            id
            profile {
              ...ProfileBase
            }
          }
        }
      }
    }
  }
}
